import { QUESTIONS as InitQuestions } from "../questions";

export const YES_ANSWER_WEIGHT = 100;
export const QUESTIONS = Object.values(InitQuestions);
export const API_URL = "http://localhost:5000/api";
export const AVG_SCORE = "/avg-score";
export const INIT_AVG_SCORE = "/init-avg-score";
